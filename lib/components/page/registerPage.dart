import 'package:flutter/material.dart';

class registerPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('สมัครสมาชิก'),
      ),
      body: const Center(
        child: Text('หน้านี้คือหน้าสมัครสมาชิก'),
      ),
    );
  }
}